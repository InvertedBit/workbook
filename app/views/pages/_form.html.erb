<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :type_id %><br />
		<%= select("page", "type_id", Type.all.collect {|p| [p.name, p.id] }) %>
   <!-- <%= f.text_field :type_id %>-->
  </div>
  <div class="field">
    <%= f.label :sorting %><br />
    <%= f.text_field :sorting %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
